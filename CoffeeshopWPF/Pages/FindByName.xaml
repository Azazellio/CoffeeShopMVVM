<Page x:Class="CoffeeshopWPF.Pages.FindByName"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CoffeeshopWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FindByName">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="MachinesGrid1" AutoGenerateColumns="False" ItemsSource="{Binding machines}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="30" Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Width="50" Header="Date" Binding="{Binding machines}"/>
                <DataGridTextColumn Width="*" Header="What's missing" Binding="{Binding machines}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
